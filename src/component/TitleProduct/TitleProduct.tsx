import { Product } from "src/models/Product"

type TitleProductProps = {
    title: string;
    products: Product[];

}

export const TitleProduct = (props: TitleProductProps) => {
    return (
        <div>
            
        </div>
    )
}